// reducer handle data for store
// update data in store

// rules: need rootReducer, reducer must return the some Value, the reducer must have some initial value

const cartData = (data = [], action) => {
  console.log(action)
}
export default cartData
